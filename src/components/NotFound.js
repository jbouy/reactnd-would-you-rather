import React from 'react';
import styled from '@emotion/styled';

const StyleContainer = styled.div`
  margin-top: 200px;
  text-align: center;
  font-size: 2em;
  font-weight: bold;
`;

const NotFound = () => <StyleContainer>404 - Page Not Found</StyleContainer>;

export default NotFound;
